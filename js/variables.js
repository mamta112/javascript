/* in case of let
let b=10;
let b=100; we cannot redeclare the same variable can reassign the value
*/

let b=10;
b=100;
console.log(b);

/*
   in case of var we can redeclare the same variable name and also reassign the value
 */
var kane="mamta";
var kane="krishna";
console.log(kane);

// To declare a constant (unchanging) variable, use const instead of let:

